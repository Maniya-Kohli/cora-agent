# Copy this file to .env and fill in your values
# Never commit .env to git

# Claude API — get from console.anthropic.com
ANTHROPIC_API_KEY=

# Serper web search — get from serper.dev (free tier available)
SERPER_API_KEY=

# Google OAuth credentials go in credentials.json + token.json (gitignored, not here)
